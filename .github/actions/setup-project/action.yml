name: Setup project
description: Install the project toolchain and dependencies.
runs:
  using: composite
  steps:
    - uses: oven-sh/setup-bun@v2
    - uses: actions/setup-node@v7
      with:
        node-version: 24
    - run: bun install --frozen-lockfile
      shell: bash
